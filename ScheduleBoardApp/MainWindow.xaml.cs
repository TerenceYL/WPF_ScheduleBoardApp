using System.Windows;

namespace ScheduleBoardApp
{
    public partial class MainWindow : Window
    {
        public MainWindow()
        {
            InitializeComponent();
            // DataContext 已於 XAML 中設定，此處不必額外指定
        }
    }
}